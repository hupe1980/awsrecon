# Enumerate iam role-trusts
```
Usage:
  awsrecon role-trusts [flags]

Flags:
  -h, --help                        help for role-trusts
      --ignore-service-link-roles   ignore service link roles

Global Flags:
  -o, --output string       output filename
      --profile string      AWS profile
      --region strings      AWS regions (default all aws regions)
  -A, --user-agent string   user-agent to use (default "awsrecon")
```